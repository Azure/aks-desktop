/node_modules
/dist
/build
/prod_deps
# All compiled JS under electron/ is generated by babel (for tests) or the build
electron/*.js
# i18next-parser config is a source JS file — keep it tracked
!electron/i18next-parser.config.js
resources/
